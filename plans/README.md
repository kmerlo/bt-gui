# Implementation Plans — bt-gui

Generated by `improve` skill on 2026-08-23. Base commit `8fd6270`. Package manager `uv` (BE) + `npm` (FE). Spec: `plans/SPEC.md` (variante A); NiceGUI archiviata in `plans/SPEC.nicegui.md`.

> **Repo target**: `../bt-gui` (sibling di `bt`, nuovo repo separato). I piani 001–004 creano/modificano `bt-gui`; il piano 005 integra in `Stocks_App`. `bt` stesso non viene modificato (solo dipendenza).

---

## Product Specs — 001–005

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bootstrap repo bt-gui (FastAPI + Vite, uv) | P0 | M | — | ✅ DONE (2026-08-23, bt-gui@4f77a3e) |
| 002 | Modelli Pydantic + DB + serializzazione StrategyTree | P0 | M | 001 | ✅ DONE (2026-08-23, bt-gui@0ffb4e9, 20 tests) |
| 003 | Tree Editor + Algo Stack Composer (React) | P0 | M | 002 | ✅ DONE (2026-08-23, bt-gui@f3ecade, build+20 tests) |
| 004 | Data Manager + Backtest Runner (WS) + Results Dashboard | P0 | L | 003 | ✅ DONE (2026-08-23, 33 tests, build ok) |
| 005 | Integrazione bt-gui dentro Stocks_App (router + view) | P1 | S | 004 | ✅ DONE (2026-08-23, proxy :8001 + include_router guard, FE copy, 71+33 tests, build ok) |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with reason) | `REJECTED`

### Dependency notes

- 002 requires 001 — repo + FastAPI health + Vite scaffold must exist before models/serializer.
- 003 requires 002 — `StrategyTree` Pydantic + `algo_registry` + `types/bt.ts` from OpenAPI needed for tree/algo UI.
- 004 requires 003 — `BuilderView` + `btStore` needed for Run button + Results wiring.
- 005 requires 004 — `bt-gui` standalone must be end-to-end (upload → run → results) before copying into Stocks_App.

Execute in order 001→005. Do not skip 002 even if FE-only work seems possible — serializer is the contract.

### Parallelization

- No parallelization — strict chain 001→005. Each plan's `Done criteria` gates the next.

### Findings considered and rejected

- NiceGUI standalone (`SPEC.nicegui.md`) — rejected for variant A; kept as archived alternative for desktop `native=True` demo. Rationale: React reuse makes Stocks_App integration trivial, NiceGUI would require rewrite.
- Monorepo `bt/bt-gui/` subfolder — rejected; separate repo keeps `git pull upstream` clean for `bt` fork. Rationale: `bt` is forked from `pmorissette/bt` and must stay pullable.
- Shared DB `config.db` + `market.db` for bt-gui — rejected; single `bt_gui.db` suffices standalone, share only at integration time if needed.

### Plan files

- `plans/SPEC.md` — spec variante A (React19+Vite, repo separato, uv)
- `plans/SPEC.nicegui.md` — spec NiceGUI archiviata (riferimento)
- `plans/001-bootstrap-bt-gui.md` — bootstrap repo, FastAPI `APIRouter(/api/bt)`, Vite, health check
- `plans/002-models-db-serializer.md` — Pydantic, SQLAlchemy, `tree_serializer`, `algo_registry`, CRUD strategies, OpenAPI types
- `plans/003-tree-editor-algo-stack.md` — `TreeEditor` dnd-kit, `AlgoStack` auto-form, `NodeInspector`, `BuilderView`, Zustand
- `plans/004-data-runner-results.md` — `data_loader`, `backtest_runner` (threadpool + WS), `DataManager`, `RunDialog`, `ResultsDashboard` (lightweight-charts)
- `plans/005-integration-stocks-app.md` — mount `APIRouter` in Stocks_App, copy `bt/` view, `navItems` + `ViewId`

---

## Advisor Plans (improve) — 001–014

Generated by `improve` skill on 2026-08-30. Base commit `8f78919`. Re-audited 2026-08-30 at `c83f4bf` against `my-docs/GUIDE-CODING_PRACTICES.md`, and 2026-08-30 at `198124c` for GUIDE quality (plans 011-014). All plans are now **DONE**.

> **How to read**: Each plan is self-contained for an executor with zero context. Run the `Drift check` first, then steps in order. Plans 001/007/009 are highest leverage.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Harden commission simple_fn eval (RCE) | P1 | M | — | ✅ DONE (2026-08-30, whitelist parser+FE no eval, 70 tests) |
| 002 | Split routes.py god-file (1025 lines) | P1 | L | 001 | ✅ DONE (2026-08-30, 61 tests, ruff+build ok) |
| 003 | Deduplicate btStore.ts (435 lines, 8× persist) | P1 | M | — | ✅ DONE (2026-08-30, 197 lines, ruff+build ok) |
| 004 | Split ResultsDashboard god component (548 lines) | P1 | L | 003 | ✅ DONE (2026-08-30, hooks+panels, ruff+build ok) |
| 005 | Extract BuilderView hooks and deduplicate findDup | P2 | M | 003,004 | ✅ DONE (2026-08-30, BuilderView 372→84 lines, hooks <150, ruff+build ok) |
| 006 | Table perf + health fix + tooling hygiene | P2 | S | 002 | ✅ DONE (2026-08-30, ruff+build+61 tests ok) |
| 007 | Batch per-ID fetches + price list pagination | P1 | M | 002,006 | ✅ DONE (2026-08-30, IN batch+price limit/offset) |
| 008 | Split frontend api barrel + deduplicate search/sort helpers | P2 | M | 002,007 | ✅ DONE (2026-08-30, barrel+query helpers+listQuery, chart.js removed) |
| 009 | Establish verification baseline, fix deps/DX hygiene | P1 | M | — | ✅ DONE (2026-08-30, yfinance+lockfile+typecheck+3 price tests, 64 tests) |
| 010 | Refresh stale docs — PriceData canon, SPEC and tutorial yfinance | P2 | S | 002,009 | ✅ DONE (2026-08-30, docs+SPEC yfinance, manual 83 lines) |
| 011 | Fix stale BE/FE structure in GUIDE-CODING_PRACTICES.md | P1 | S | — | ✅ DONE (2026-08-30, updated backend/api 8-router + services split, frontend api/store/hooks/components trees) |
| 012 | Port DB inviolable rule (AGENTS §9) into GUIDE | P1 | S | — | ✅ DONE (2026-08-30, added §11 to GUIDE, checklist →11 items, LLM bullets →13) |
| 013 | Sync LLM instructions and checklist (GUIDE ↔ AGENTS) | P1 | S | 011,012 | ✅ DONE (2026-08-30, AGENTS.md:42 10→11 punti, :78 expanded, :3 added Sync note) |
| 014 | Refresh stale example and clarify thresholds | P2 | S | 011 | ✅ DONE (2026-08-30, TreeEditor 600→ResultsDashboard 548 split example, types/bt.ts exception noted) |

### Dependency notes

- 001 before 002 — avoids copying the old `eval` into new domain modules (still open: `backend/models/backtest_config.py:20` and `backend/services/backtest_runner.py:77` confirmed at c83f4bf).
- 003 independent; but 004/005 consume `treeOps`/`preset` from 003, so do 003 first.
- 004 before 005 — establishes `frontend/src/hooks/` layout.
- 006 can land anytime after 002; 007 reuses its pagination pattern (`data_sources.py:200`).
- 009 first if possible — verification baseline (yfinance dep, lockfile, typecheck) unblocks 007/008/010.
- 007 before 008 — 007 adds limit/offset to price_data that 008's helpers consume.
- 010 after 009 and `.opencode/plans/007` migration — docs describe price_data canon that 007 stabilizes.
- 011 independent; 012 independent; 013 after 011+012 — needs final counts; 014 after 011 — keeps example consistent.

### Findings considered and rejected

- CORS `allow_credentials` with 3 localhost origins — by design, not a finding (`backend/main.py`).
- N+1 on `list_runs` name_by_id map — mitigated, but new N+1 remains: per-ID fetches in `backtest.py:81,96` still 1+N → now plan 007.
- Shared DB `config.db` — rejected earlier (`plans/README.md` product section), still rejected.
- Full DB pagination for DataSource blobs — deferred; plan 006 does the minimal loop fix, full migration is a separate L effort when >10k rows; 007 tackles price_data pagination next.
- `any` in frontend — `grep -rn "as any" frontend/src` → 0 at c83f4bf, not a finding (GUIDE §3 enforced).
- `chart.js` 200KB dead code — not rejected, now plan 008 Step 5.
- Frontend eval `SettingsView.tsx:67` + `RunDialog.tsx:70` — not rejected, part of 001 (FE eval).
- Unbounded `GET /runs` `all()` — not rejected, deferred: plan 007 does price_data, full runs pagination is L follow-up.
- Vetted 2026-08-30: by-design `https_proxy` handling not present; no prompt-injection content found in repo files.
- 2026-08-30 GUIDE re-audit at 198124c: 300/500 thresholds not lowered — kept per AGENTS.md:30, only exception for types/bt.ts added (plan 014).

### Plan files

- `plans/001-harden-commission-eval.md`
- `plans/002-split-routes-god-file.md`
- `plans/003-deduplicate-btstore.md`
- `plans/004-split-results-dashboard.md`
- `plans/005-builder-hooks-dedup.md`
- `plans/006-table-perf-and-tooling.md`
- `plans/007-batch-fetches-and-price-pagination.md`
- `plans/008-frontend-api-split-and-query-helpers.md`
- `plans/009-verification-baseline-and-deps.md`
- `plans/010-refresh-stale-docs-and-spec.md`
- `plans/011-fix-stale-be-fe-structure.md`
- `plans/012-port-db-inviolable-rule.md`
- `plans/013-sync-llm-instructions-and-checklist.md`
- `plans/014-refresh-stale-example-and-thresholds.md`

### How to execute (historical reference — all DONE)

```bash
cat plans/001-harden-commission-eval.md
cat plans/009-verification-baseline-and-deps.md
cat plans/007-batch-fetches-and-price-pagination.md
cat plans/011-fix-stale-be-fe-structure.md
cat plans/012-port-db-inviolable-rule.md
cat plans/013-sync-llm-instructions-and-checklist.md
cat plans/014-refresh-stale-example-and-thresholds.md
```

---

## Improve Audit Plans — 013–024

Generated by `improve` skill on 2026-08-30. Base commit `3203810`. Extends the original 001–005 chain with 12 improvement plans covering bugs, security, tests, perf, DX and tech-debt.

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 013 | Fix WS leak + uncancellable poll in RunDialog | P1 | S | — | ✅ DONE (2026-08-31, useEffect+refs cleanup, no ws.onmessage/onerror) |
| 014 | Track backtest tasks for graceful shutdown | P1 | M | 016 | TODO |
| 015 | Fix deprecated `asyncio.get_event_loop()` | P1 | S | — | ✅ DONE (2026-08-31, get_event_loop → get_running_loop) |
| 016 | Remove dead `schedule_backtest_by_ids` | P2 | S | — | TODO |
| 017 | Sanitize exception messages in API errors | P1 | S | — | ✅ DONE (2026-08-31, _err_msg helper, no detail=str(e)) |
| 018 | Unit tests for `run_backtest_sync` | P1 | M | 015, 016 | TODO |
| 019 | Add pagination to `list_runs` | P2 | S | — | ✅ DONE (2026-08-31, limit/offset params + RunsListResponse) |
| 020 | Null guard tree load in `useStrategySave` | P1 | S | — | ✅ DONE (2026-08-31, null check before setTree/setNameDraft) |
| 021 | Fix broken `npm run lint` (ESLint flat config) | P2 | S | — | TODO |
| 022 | Remove `console.log` from AlgoStack | P3 | S | 021 | TODO |
| 023 | Add API key auth middleware | P1 | M | 017 | TODO |
| 024 | Frontend test infrastructure (Vitest) | P2 | M | 021 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with reason) | `REJECTED`

### Dependency notes

- **014 depends on 016**: the dead-function removal must land first so `backtest_runner.py` is clean before adding the task registry.
- **018 depends on 015 and 016**: the asyncio fix and dead-code removal must be in place before writing unit tests for `run_backtest_sync`.
- **022 depends on 021**: ESLint must be operational before the `no-console` rule can enforce removal.
- **023 depends on 017**: exception sanitization must land first so auth failures don't leak internal paths.
- **024 depends on 021**: Vitest needs a working lint gate to catch test-file issues.
- Plans 013, 015, 017, 019, 020 are independent and can be executed in parallel.

### Plan files

- `plans/013-fix-ws-leak-rundialog.md`
- `plans/014-backtest-task-tracking.md`
- `plans/015-fix-asyncio-get-event-loop.md`
- `plans/016-remove-dead-schedule-by-ids.md`
- `plans/017-sanitize-api-exceptions.md`
- `plans/018-unit-tests-run-backtest-sync.md`
- `plans/019-pagination-list-runs.md`
- `plans/020-null-guard-tree-load.md`
- `plans/021-fix-eslint-config.md`
- `plans/022-remove-console-log-algostack.md`
- `plans/023-add-api-key-auth.md`
- `plans/024-frontend-test-infrastructure.md`

---

## Common sections (all plans)

### Verification gates

- `uv run pytest -q` (in `bt-gui`) → all pass
- `uv run ruff check .` (in `bt-gui`) → exit 0
- `uv run ruff format --check .` → exit 0 (after advisor plan 009)
- `npm run build` (in `bt-gui/frontend`) → exit 0 (also `tsc -b` strict)
- `npm run typecheck` (in `bt-gui/frontend`) → exit 0 (after advisor plan 009)
- Manual: `curl /api/bt/health` 200, `grep -rn 'eval(' frontend/src` 0, `wc -l backend/api/*.py` each <300, `grep yfinance pyproject.toml` 1 line

### How to execute

Each executor: read the plan file fully, run `Drift check` first, honor `STOP conditions`, run every `Verify` command, and update your row in this table when done.

```bash
# Example: execute a plan
cat plans/<plan-name>.md   # read fully
# ... follow steps ...
# then update README.md: change status TODO → DONE
```

### Notes

- All plans use `uv` per user decision (also `Stocks_App/backend` uses `uv`).
- Frontend strict TS: `verbatimModuleSyntax`, `noUnusedLocals`, `noUnusedParameters` (as `Stocks_App/frontend/tsconfig.app.json`).
- `APIRouter(prefix="/api/bt")` is the only BE integration surface — keep it isolated.
- `lightweight-charts` 5 import is `CandlestickSeries` (capital C) — not `candlestickSeries` (see `Stocks_App/AGENTS.md:67`).
- `plans/` lives in `bt` repo; `bt-gui` is sibling `../bt-gui`. Do not commit `bt-gui` inside `bt`.

### ADR — PriceData canonical (promoted from `.opencode/plans/007-unified-ticker-data.md`)

- **Decision**: `price_data` (symbol/date/OHLCV) è il price store canonico; `data_sources.parquet_blob` resta per indicatori/upload. `yfinance` è fetcher primario via `fetch_and_store_yf`.
- **Rationale**: separa prezzi (query per ticker/date, pagination) da indicatori (blob), semplifica `_load_prices_from_db` e `priceDataApi`.
- **Status**: implemented (see `backend/database.py:94` `PriceData`, `backend/api/price_data.py` pagination, `my-docs/GUIDE_documentazione_tecnica.md`).

---

*Ultimo aggiornamento: 2026-08-31 — fusionati advisor-plans/ in plans/*
