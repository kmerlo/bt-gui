# Implementation Plans — bt-gui (Variante A, React + Vite, repo separato)

Generated by `improve` skill on 2026-08-23. Base commit `8fd6270`. Package manager `uv` (BE) + `npm` (FE). Spec: `plans/SPEC.md` (variante A); NiceGUI archiviata in `plans/SPEC.nicegui.md`.

> **Repo target**: `../bt-gui` (sibling di `bt`, nuovo repo separato). I piani 001–004 creano/modificano `bt-gui`; il piano 005 integra in `Stocks_App`. `bt` stesso non viene modificato (solo dipendenza).

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Bootstrap repo bt-gui (FastAPI + Vite, uv) | P0 | M | — | ✅ DONE (2026-08-23, bt-gui@4f77a3e) |
| 002 | Modelli Pydantic + DB + serializzazione StrategyTree | P0 | M | 001 | ✅ DONE (2026-08-23, bt-gui@0ffb4e9, 20 tests) |
| 003 | Tree Editor + Algo Stack Composer (React) | P0 | M | 002 | ✅ DONE (2026-08-23, bt-gui@f3ecade, build+20 tests) |
| 004 | Data Manager + Backtest Runner (WS) + Results Dashboard | P0 | L | 003 | ✅ DONE (2026-08-23, 33 tests, build ok) |
| 005 | Integrazione bt-gui dentro Stocks_App (router + view) | P1 | S | 004 | ✅ DONE (2026-08-23, proxy :8001 + include_router guard, FE copy, 71+33 tests, build ok) |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with reason) | `REJECTED`

## Dependency notes

- 002 requires 001 — repo + FastAPI health + Vite scaffold must exist before models/serializer.
- 003 requires 002 — `StrategyTree` Pydantic + `algo_registry` + `types/bt.ts` from OpenAPI needed for tree/algo UI.
- 004 requires 003 — `BuilderView` + `btStore` needed for Run button + Results wiring.
- 005 requires 004 — `bt-gui` standalone must be end-to-end (upload → run → results) before copying into Stocks_App.

Execute in order 001→005. Do not skip 002 even if FE-only work seems possible — serializer is the contract.

## Git workflow (trunk-based, come Stocks_App)

- Lavoro sempre su `master`, nessun branch `feat/*`.
- `git pull origin master` prima di ogni task, `git push origin master` dopo il commit locale.
- PR manuali facoltative se l'utente le richiede; di default push diretto su `master`.
- Rimuovere branch vecchi quando completati: `git branch -d feat/… && git push origin --delete feat/…`.

## Plan files

- `plans/SPEC.md` — spec variante A (React19+Vite, repo separato, uv)
- `plans/SPEC.nicegui.md` — spec NiceGUI archiviata (riferimento)
- `plans/001-bootstrap-bt-gui.md` — bootstrap repo, FastAPI `APIRouter(/api/bt)`, Vite, health check
- `plans/002-models-db-serializer.md` — Pydantic, SQLAlchemy, `tree_serializer`, `algo_registry`, CRUD strategies, OpenAPI types
- `plans/003-tree-editor-algo-stack.md` — `TreeEditor` dnd-kit, `AlgoStack` auto-form, `NodeInspector`, `BuilderView`, Zustand
- `plans/004-data-runner-results.md` — `data_loader`, `backtest_runner` (threadpool + WS), `DataManager`, `RunDialog`, `ResultsDashboard` (lightweight-charts)
- `plans/005-integration-stocks-app.md` — mount `APIRouter` in Stocks_App, copy `bt/` view, `navItems` + `ViewId`

## Parallelization

- No parallelization — strict chain 001→005. Each plan's `Done criteria` gates the next.

## Findings considered and rejected

- NiceGUI standalone (`SPEC.nicegui.md`) — rejected for variant A; kept as archived alternative for desktop `native=True` demo. Rationale: React reuse makes Stocks_App integration trivial, NiceGUI would require rewrite.
- Monorepo `bt/bt-gui/` subfolder — rejected; separate repo keeps `git pull upstream` clean for `bt` fork. Rationale: `bt` is forked from `pmorissette/bt` and must stay pullable.
- Shared DB `config.db` + `market.db` for bt-gui — rejected; single `bt_gui.db` suffices standalone, share only at integration time if needed.

## How to execute

Each executor: read the plan file fully, run `Drift check` first, honor `STOP conditions`, run every `Verify` command, and update your row in this table when done.

```bash
# Example: execute plan 001
cat plans/001-bootstrap-bt-gui.md   # read fully
# ... follow steps ...
# then update README.md: change 001 status TODO → DONE
```

## Verification gates (all plans)

- `uv run pytest -q` (in `bt-gui`) → all pass
- `uv run ruff check .` (in `bt-gui`) → exit 0
- `npm run build` (in `bt-gui/frontend` and, for 005, `Stocks_App/frontend`) → exit 0
- Manual E2E per plan's `Done criteria` (health, drag-drop, run, results, integration hash `#bt-builder`)

## Notes

- All plans use `uv` per user decision (also `Stocks_App/backend` uses `uv`).
- Frontend strict TS: `verbatimModuleSyntax`, `noUnusedLocals`, `noUnusedParameters` (as `Stocks_App/frontend/tsconfig.app.json`).
- `APIRouter(prefix="/api/bt")` is the only BE integration surface — keep it isolated.
- `lightweight-charts` 5 import is `CandlestickSeries` (capital C) — not `candlestickSeries` (see `Stocks_App/AGENTS.md:67`).
- `plans/` lives in `bt` repo; `bt-gui` is sibling `../bt-gui`. Do not commit `bt-gui` inside `bt`.

## ADR — PriceData canonical (promoted from `.opencode/plans/007-unified-ticker-data.md`)

- **Decision**: `price_data` (symbol/date/OHLCV) è il price store canonico; `data_sources.parquet_blob` resta per indicatori/upload. `yfinance` è fetcher primario via `fetch_and_store_yf`.
- **Rationale**: separa prezzi (query per ticker/date, pagination) da indicatori (blob), semplifica `_load_prices_from_db` e `priceDataApi`.
- **Status**: implemented (see `backend/database.py:94` `PriceData`, `backend/api/price_data.py` pagination, `my-docs/GUIDE_documentazione_tecnica.md`).

---

# Improve Audit Plans — 2026-08-30

Generated by `improve` skill on 2026-08-30. Base commit `3203810`. Extends the original 001–005 chain with 12 improvement plans covering bugs, security, tests, perf, DX and tech-debt.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 013 | Fix WS leak + uncancellable poll in RunDialog | P1 | S | — | TODO |
| 014 | Track backtest tasks for graceful shutdown | P1 | M | 016 | TODO |
| 015 | Fix deprecated `asyncio.get_event_loop()` | P1 | S | — | TODO |
| 016 | Remove dead `schedule_backtest_by_ids` | P2 | S | — | TODO |
| 017 | Sanitize exception messages in API errors | P1 | S | — | TODO |
| 018 | Unit tests for `run_backtest_sync` | P1 | M | 015, 016 | TODO |
| 019 | Add pagination to `list_runs` | P2 | S | — | TODO |
| 020 | Null guard for tree load in `useStrategySave` | P1 | S | — | TODO |
| 021 | Fix broken `npm run lint` (ESLint flat config) | P2 | S | — | TODO |
| 022 | Remove `console.log` from AlgoStack | P3 | S | 021 | TODO |
| 023 | Add API key auth middleware | P1 | M | 017 | TODO |
| 024 | Frontend test infrastructure (Vitest) | P2 | M | 021 | TODO |

Status values: `TODO` | `IN PROGRESS` | `DONE` | `BLOCKED` (with reason) | `REJECTED`

## Dependency notes

- **014 depends on 016**: the dead-function removal must land first so `backtest_runner.py` is clean before adding the task registry.
- **018 depends on 015 and 016**: the asyncio fix and dead-code removal must be in place before writing unit tests for `run_backtest_sync`.
- **022 depends on 021**: ESLint must be operational before the `no-console` rule can enforce removal.
- **023 depends on 017**: exception sanitization must land first so auth failures don't leak internal paths.
- **024 depends on 021**: Vitest needs a working lint gate to catch test-file issues.
- Plans 013, 015, 017, 019, 020 are independent and can be executed in parallel.

## Plan files

- `plans/013-fix-ws-leak-rundialog.md`
- `plans/014-backtest-task-tracking.md`
- `plans/015-fix-asyncio-get-event-loop.md`
- `plans/016-remove-dead-schedule-by-ids.md`
- `plans/017-sanitize-api-exceptions.md`
- `plans/018-unit-tests-run-backtest-sync.md`
- `plans/019-pagination-list-runs.md`
- `plans/020-null-guard-tree-load.md`
- `plans/021-fix-eslint-config.md`
- `plans/022-remove-console-log-algostack.md`
- `plans/023-add-api-key-auth.md`
- `plans/024-frontend-test-infrastructure.md`
