# Plan 014: Refresh stale example and clarify thresholds

> **Executor instructions**: Follow this plan step by step. Run every
> verification command and confirm the expected result before moving to the
> next step. If anything in the "STOP conditions" section occurs, stop and
> report — do not improvise. When done, update the status row for this plan
> in `advisor-plans/README.md` — unless a reviewer dispatched you and told you they
> maintain the index.
>
> **Drift check (run first)**: `git diff --stat 198124c..HEAD -- my-docs/GUIDE-CODING_PRACTICES.md frontend/src/bt/components frontend/src/types/bt.ts`
> If any in-scope file changed since this plan was written, compare the
> "Current state" excerpts against the live code before proceeding; on a
> mismatch, treat it as a STOP condition.

## Status

- **Priority**: P2
- **Effort**: S (1h)
- **Risk**: LOW
- **Depends on**: `advisor-plans/011-fix-stale-be-fe-structure.md` (needs updated file trees to keep example consistent)
- **Category**: docs / tech-debt
- **Planned at**: commit `198124c`, 2026-08-30

## Why this matters

`GUIDE:54-56` uses `TreeEditor.tsx (600+ righe)` as the anti-pattern, but `TreeEditor.tsx:98` at `198124c` is already small. The most recent real split is `ResultsDashboard.tsx:548→69` (`appunti_sviluppi.md:32`, `advisor-plans/004-split-results-dashboard.md` DONE). Keeping a stale example makes the 300/500 thresholds feel hypothetical and reduces compliance. Also `GUIDE:64-66` says “mai superare 500” without excepting `frontend/src/types/bt.ts:439` (auto-generated, 439 lines, legit >300). Clarifying the exception prevents false alarms and aligns with `AGENTS.md:30` (300 pratica, 500 dura).

## Current state

Relevant files, each with one line on its role:
- `my-docs/GUIDE-CODING_PRACTICES.md:54-66` — Rule 1 “Un file, una responsabilità” with example TreeEditor 600 and thresholds 300 pratica / 500 dura.
- `frontend/src/bt/components/TreeEditor.tsx` — 98 lines at 198124c (small, not 600).
- `frontend/src/bt/components/ResultsDashboard.tsx` — 69 lines orchestration after split; hooks `useRunsTable.ts:110`, `useEquityCharts.ts:107` etc.
- `frontend/src/types/bt.ts` — 439 lines, auto-generated by `openapi-typescript`, correctly over 300 but exempt.
- `my-docs/appunti_sviluppi.md:32` — records the real split as exemplar.

Excerpts (as at 198124c):

`GUIDE:54-66`:
```
```
❌ TreeEditor.tsx (600+ righe)
   └── drag logic, node render, inspector panel, algo stack inline...

✅ hooks/useTreeDrag.ts    (~150 righe) — solo logica drag-and-drop
...
```

**Soglia pratica**: se un file supera le **300 righe**, chiediti: "Quali di queste responsabilità
posso estrarre?"
**Soglia dura**: **mai superare le 500 righe**. Se ci arrivi, estrai ORA.
```

`wc -l` at 198124c (top 5):
```
   98 frontend/src/bt/components/TreeEditor.tsx
  190 frontend/src/bt/store/btStore.ts
  247 frontend/src/bt/components/DataManager.tsx
  261 frontend/src/bt/components/DataDetailView.tsx
  439 frontend/src/types/bt.ts  # auto-generated
```

Repo conventions that apply:
- Exemplars should be live: use the most recent split that paid off (ResultsDashboard) rather than a fictional 600-line file.
- Thresholds are otherwise kept per `AGENTS.md:30` — this plan only clarifies the generated-file exception, not changing numbers.

## Commands you will need

| Purpose | Command | Expected on success |
|---------|---------|---------------------|
| Frontend build | `npm run build --prefix frontend` | exit 0 |
| Backend lint | `uv run ruff check .` | exit 0 |
| Tests | `uv run pytest -q` | all pass |
| Verify example updated | `grep -n "ResultsDashboard.*548" my-docs/GUIDE-CODING_PRACTICES.md` | 1 match |
| Verify exception noted | `grep -n "types/bt.ts.*generato.*eccezione\|eccezione.*types/bt.ts" my-docs/GUIDE-CODING_PRACTICES.md` | 1 match |
| Verify old example gone | `grep -n "TreeEditor.*600" my-docs/GUIDE-CODING_PRACTICES.md` | 0 matches |

## Scope

**In scope**:
- `my-docs/GUIDE-CODING_PRACTICES.md` — Rule 1 example + thresholds paragraph.

**Out of scope**:
- `frontend/src/types/bt.ts`, `frontend/src/bt/components/*` — code is fine; only docs.
- `AGENTS.md` — already has thresholds at line 30; keep as is (no duplication).
- Any numeric threshold change (300/500 stays).

## Git workflow

- Trunk-based `master`. Commit `docs: refresh stale example and clarify thresholds`.
- One commit.

## Steps

### Step 1: Replace stale TreeEditor 600 example with live ResultsDashboard 548→69

Replace `GUIDE:54-62` block:

From:
```
❌ TreeEditor.tsx (600+ righe)
   └── drag logic, node render, inspector panel, algo stack inline...

✅ hooks/useTreeDrag.ts    (~150 righe) — solo logica drag-and-drop
✅ hooks/useTreeInspector.ts (~100 righe) — solo stato e logica dell'inspector
✅ components/NodeCard.tsx  (~60 righe)  — rendering singolo nodo
✅ components/AlgoStack.tsx (~120 righe) — composizione algo (già separato)
```
To (use live numbers, reference appunti and plan 004):
```
❌ ResultsDashboard.tsx (548 righe prima di 2026-08-30)
   └── filtri, fetch, polling, charts, metrics, transactions in un unico file

✅ hooks/useRunsTable.ts     (~110 righe) — filtri/sort/refresh/polling
✅ hooks/useEquityCharts.ts   (~107 righe) — sanitize/drawdown + sync crosshair/pan
✅ components/RunsTable.tsx   (~121 righe) — tabella sticky + filtri
✅ components/MetricsPanel.tsx (~69 righe) — grouping Daily/Monthly/Yearly
✅ Orchestratore ResultsDashboard.tsx (~69 righe) — solo composizione

> Esempio vivo: vedi appunti_sviluppi.md:32 e advisor-plans/004-split-results-dashboard.md
```
Keep the ❌/✅ formatting and code-fence style.

**Verify**: `grep -n "ResultsDashboard.*548" my-docs/GUIDE-CODING_PRACTICES.md` → 1; `grep -n "TreeEditor.*600" my-docs/GUIDE-CODING_PRACTICES.md` → 0.

### Step 2: Clarify thresholds with generated-file exception

Replace `GUIDE:64-66`:
```
**Soglia pratica**: se un file supera le **300 righe**, chiediti: "Quali di queste responsabilità
posso estrarre?"
**Soglia dura**: **mai superare le 500 righe**. Se ci arrivi, estrai ORA.
```
With:
```
**Soglia pratica**: se un file supera le **300 righe**, chiediti: "Quali di queste responsabilità
posso estrarre?"
**Soglia dura**: **mai superare le 500 righe**. Se ci arrivi, estrai ORA.
> **Eccezione**: `frontend/src/types/bt.ts` (439 righe, AUTO-GENERATO da openapi-typescript) è escluso — non splittarlo.

```
Keep blank line after.

**Verify**: `grep -n "Eccezione.*types/bt.ts" my-docs/GUIDE-CODING_PRACTICES.md` → 1; `grep -n "AUTO-GENERATO" my-docs/GUIDE-CODING_PRACTICES.md` still present elsewhere.

### Step 3: Full verification

**Verify**: `npm run build --prefix frontend` → exit 0; `uv run ruff check .` → exit 0; `uv run pytest -q` → all pass.

## Test plan

No code tests. Docs verification as above. Check that `wc -l frontend/src/bt/components/ResultsDashboard.tsx` ≤80 after change (still 69).

## Done criteria

Machine-checkable. ALL must hold:
- [ ] `grep -c "TreeEditor.*600" my-docs/GUIDE-CODING_PRACTICES.md` == 0
- [ ] `grep -c "ResultsDashboard.*548" my-docs/GUIDE-CODING_PRACTICES.md` ≥1
- [ ] `grep -c "Eccezione.*types/bt.ts" my-docs/GUIDE-CODING_PRACTICES.md` ≥1
- [ ] `npm run build --prefix frontend` exits 0
- [ ] `uv run ruff check .` exits 0
- [ ] Only `my-docs/GUIDE-CODING_PRACTICES.md` modified

## STOP conditions

Stop and report back (do not improvise) if:
- `frontend/src/bt/components/ResultsDashboard.tsx` is no longer ~69 lines (e.g. reverted to 548) — example would be stale again.
- `frontend/src/types/bt.ts` line count drops below 300 (generated file changed) — exception wording would be unnecessary; report.
- A step's verification fails twice.
- The fix appears to require touching `AGENTS.md` or code files.

## Maintenance notes

- When a new large split lands, update this example to the latest most impactful split (keep it live, not fictional).
- Reviewers should verify `wc -l` numbers in the example match the claimed files at HEAD; stale numbers are the very bug this plan fixes.
- Deferred: automating `wc -l` check in CI — S effort, not now.
